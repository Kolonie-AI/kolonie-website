---
/**
 * EditLink, on documentation pages only (kolonie-website#21).
 *
 * See `src/lib/chrome.ts` for the rule and the reasoning. This file exists to
 * apply it and holds no judgement of its own.
 */

import Default from '@astrojs/starlight/components/EditLink.astro'
import { isDocumentation } from '../../lib/chrome'

const documentation = isDocumentation(Astro.locals.starlightRoute.id)
---

{documentation && <Default><slot /></Default>}
